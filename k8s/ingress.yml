---
# Ingress — no rewrite rules needed; each pod's nginx sidecar handles its own prefix.
#
# nginx-ingress controller (default):
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mapwatch-ingress
spec:
  rules:
    - http:
        paths:
          - path: /department/mapwatch-py
            pathType: Prefix
            backend:
              service:
                name: mapwatch-py
                port:
                  number: 80
          - path: /department/tilemap-server
            pathType: Prefix
            backend:
              service:
                name: tilemap-server
                port:
                  number: 80

# ── Traefik IngressRoute alternative (uncomment if using Traefik) ─────────────
# apiVersion: traefik.io/v1alpha1
# kind: IngressRoute
# metadata:
#   name: mapwatch-ingress
# spec:
#   entryPoints: [web]
#   routes:
#     - match: PathPrefix(`/department/mapwatch-py`)
#       kind: Rule
#       services:
#         - name: mapwatch-py
#           port: 80
#     - match: PathPrefix(`/department/tilemap-server`)
#       kind: Rule
#       services:
#         - name: tilemap-server
#           port: 80
