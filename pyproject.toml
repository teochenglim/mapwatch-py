[project]
name = "mapwatch-py"
version = "0.1.0"
description = "mapwatcher python version"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.135.1",
    "httpx>=0.28.1",
    "uvicorn[standard]>=0.42.0",
    "jinja2>=3.1.0",
    "pyyaml>=6.0",
]
